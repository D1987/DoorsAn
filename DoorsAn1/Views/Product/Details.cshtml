@using System
@using DoorsAn1.ViewModels
@model ProductListViewModel
@{
    ViewBag.Title = @Model.Product.Name;
}
<h2>Товар @Model.Product.Name</h2>
<div>
    <dl class="dl-horizontal">
        <dt>Категория</dt>
        <dd>@Model.Category.CategoryName</dd>
        <dt>Производитель</dt>        
        <dd>@Model.Product.Producer</dd>
        <dt>Цена</dt>
        <dd>@Model.Product.Price</dd>
        <dt>Описание</dt>
        <dd>@Model.Product.LongDescription</dd>
        <dt>Статус</dt>
        <dd>@if (Model.Product.Status.Equals(true)) {
                <p>В наличии</p>
            } else {<p>В наличии нет</p> }</dd>
    </dl>
</div>
<div>

    @if (Model.Product.Image != null)
    {
        <img style='width:80px; height:60px;' src="data:image/jpeg;base64,@(Convert.ToBase64String(Model.Product.Image))" />
    }
</div>