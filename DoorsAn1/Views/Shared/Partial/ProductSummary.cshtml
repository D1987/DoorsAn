
  <div class="col-sm-4 col-md-4 col-lg-4">
      <div class="thumbnail">
          <a asp-action="Details" asp-controller="Product" asp-route-id="@Model.ProductId" style="text-decoration: none">
              <div class="item-responsive item-4by3">
                  @if (Model.Image != null)
                  {
                      <div class="content" style="background: url(data:image/jpeg;base64,@(Convert.ToBase64String(Model.Image)));">
                          @if (Model.InStock != 0)
                          {
                            <div class="discount">-@Model.InStock%</div>
                          }
                      </div>
                  }
                  else
                  {
                      <div class="content" style="background: url(../../Images/default.png);">
                          @if (Model.InStock != 0)
                          {
                              <div class="discount">-@Model.InStock%</div>
                          }
                      </div>
                  }
              </div>
            <div class="caption">   
                <p class="pull-right" id="price">@Model.Price руб</p>
                <h2 id="title">@Model.Name</h2>             
                
                @if (!Model.Status.Equals(true))
                {
                    <p id="statusFalse">В наличии нет</p>
                }else
                {
                    <p id="statusTrue">В наличии</p>
                }               
            </div>
          </a>
      </div>
</div>

